actor: ApiTester
modules:
    enabled:
        - \Helper\Api
        - REST:
            url: /api/
            depends: Symfony
        - Symfony:
            app_path: 'src'
            environment: 'test'

